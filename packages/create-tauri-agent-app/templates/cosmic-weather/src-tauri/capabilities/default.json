{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the Cosmic Weather window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "agent-runtime:operator"
  ]
}
